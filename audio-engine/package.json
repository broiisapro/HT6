{
  "name": "audio-engine",
  "version": "0.1.0",
  "private": true,
  "description": "Human MIDI audio engine: contract WebSocket server + royalty-free (CC0) instrumental beds, randomly picked per mood zone, on loop.",
  "type": "module",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "fetch-beds": "node scripts/fetch-beds.js"
    "generate-bed": "node scripts/generate-bed.js",
    "prepare-stems": "node scripts/prepare-stems.js",
    "generate-stems": "node scripts/generate-stems.js",
    "test": "node --test test/mapper.test.js"
  },
  "dependencies": {
    "node-web-audio-api": "^1.0.9",
    "ws": "^8.18.0"
  }
}
